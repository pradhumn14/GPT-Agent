import React from "react";

const SendButton = () => {
  return (
    <>
      <button id="sendBtn" className="btn btn-primary" type="submit">Send</button>
    </>
  );
};

export default SendButton;
